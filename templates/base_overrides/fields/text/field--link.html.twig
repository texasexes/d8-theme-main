{% extends "field.html.twig" %}

{% block item_block %}

  {% set linkContent = linkContent|default(item.content['#title']) %}

  {% set linkAttributes = [] %}
  {% set linkAttributes = linkAttributes|merge({"href" : (item.content['#url']|render)}) %}
  {% if item.attributes %}
    {% set linkAttributes = linkAttributes|merge(item.attributes) %}
  {% endif %}

    {% include "@atoms/01-links/link/link.twig" with {
      "link_content": linkContent,
      "link_attributes": linkAttributes,
    } %}

{% endblock item_block%}

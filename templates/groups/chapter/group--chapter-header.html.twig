{#
/**
 * @file
 * Theme implementation to display a chapter group in a page header.
 *
 * This template does not extend group.html.twig because group.html.twig
 * does not define any blocks!
 */
#}

{% set image %}
  {{ content.field_chapter_featured_image }}
{% endset %}
{% set css = 'background-image:url(' ~ (image|striptags) ~ ');' %}
{% set attributes = attributes.setAttribute('style', css) %}

<div{{ attributes }}>

  <div class="group-header-title">
    {{ content.label }}
  </div>

  {#
    TODO: If Field Group module is added to this project, these fields would
    not have to be hardcoded here this way.
  #}
  <div class="group-header-links">
    {{ content.field_chapter_facebook_page }}
    {{ content.field_chapter_twitter }}
    {{ content.field_chapter_instagram }}
    {{ content.field_chapter_linkedin_page }}
  </div>

</div>

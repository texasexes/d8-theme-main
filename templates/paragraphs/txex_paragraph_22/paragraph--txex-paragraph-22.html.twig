{% extends "paragraph.html.twig" %}

{% block paragraph %}

  {% set paragraph_22_items = [] %}
  {% set tempArray5 = [] %}

  {% for key, child in content['field_txex_ref_rev_para_01'] if key|first != '#' %}
    {% set paragraph_22_items = paragraph_22_items|merge({ (key) : (child)}) %}
  {% endfor %}

  {% set layout_level = layout_level + 1 |default(1) %}
  {% set tempArray5 = tempArray5|merge({'layout_level' : ('layout-level-'~layout_level),}) %}
  {% set tempArray5 = tempArray5|merge({'paragraph-wrapper' : 'paragraph-wrapper', 'layout-row' : 'layout-row'}) %}

  {% if paragraph_modifiers %}
    {% set tempArray5 = tempArray5|merge(paragraph_modifiers) %}
  {% endif %}

  {% include "@base/layouts/layout/00-layout.twig" with {
    "layout_attributes": attributes.addClass(classes),
    "layout_base_class": paragraph_base_class,
    "layout_modifiers": tempArray5,
    "layout_items": paragraph_22_items,
    "layout_modifiers_additional": {1 : 'vertical'}
  }%}

{% endblock paragraph %}

{# https://drupal.stackexchange.com/questions/208723/iterate-through-multiple-value-content-field-in-twig-template
https://drupal.stackexchange.com/questions/174742/loop-through-child-elements-in-twig-like-elementchildren
#}
